<h2>Field Reports <%= link_to image_tag("plus-circle.png", size: "20"), new_post_path%></h2>

<ul>
  <% @posts.each do |post| %>
    <li>
      <%= link_to(post.title, post_path(post))%>
      </a>
    </li>
  <% end %>
</ul>
